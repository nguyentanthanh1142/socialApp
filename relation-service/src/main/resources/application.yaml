server:
  port: 8086
  servlet:
    context-path: /relation
spring:
  application:
    name: relation-service
  data:
    mongodb:
      uri: mongodb://root:root@localhost:27017/relation-service?authSource=admin
app:
  services:
    profile:
      url: http://localhost:8081/profile
    identity:
      url: http://localhost:8080/identity
